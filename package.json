{
  "name": "fdf",
  "private": true,
  "version": "1.0.0",
  "description": "Free Delivery Forever — order-clubbing monorepo",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"npm run dev -w api\" \"npm run dev -w web\"",
    "build": "npm run build -w api && npm run build -w web",
    "lint": "npm run lint -w api && npm run lint -w web"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
